if (canUseWebP() || ($("*").each(function (e, t) {
        $(t).css("background-image").length > 4 && $(t).css("background-image", $(t).css("background-image").replace(".webp", ".png"))
    }), $("img").each(function () {
        try {
            $(this).attr("src", $(this).attr("src").replace(".webp", ".png"))
        } catch (e) {}
        try {
            $(this).attr("data-src", $(this).attr("data-src").replace(".webp", ".png"))
        } catch (e) {}
    })), "IntersectionObserver" in window && !("isIntersecting" in window.IntersectionObserverEntry.prototype)) $("*[data-src]").each(function () {
    var e = $(this).attr("data-src");
    e && $(this).attr("src", e)
});

function canUseWebP() {
    var e = document.createElement("canvas");
    return !(!e.getContext || !e.getContext("2d")) && 0 == e.toDataURL("image/webp").indexOf("data:image/webp")
}
